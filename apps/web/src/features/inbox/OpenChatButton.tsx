/** Placeholder. Owned by the leads-inbox workstream: opens (or starts) the WhatsApp conversation for a lead or contact. */
export function OpenChatButton(_props: { leadId?: string; contactId?: string; className?: string }) {
  return null;
}
