/** Placeholder. Owned by the pipeline workstream: deals of a contact or company, with "New deal". */
export function ContactDealsPanel(_props: { contactId?: string; companyId?: string }) {
  return null;
}
