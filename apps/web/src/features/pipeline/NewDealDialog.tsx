/** Placeholder. Owned by the pipeline workstream: create a deal, optionally prefilled with a contact/company. */
export function NewDealDialog(_props: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  defaults?: { contactId?: string; companyId?: string };
  onCreated?: (dealId: string) => void;
}) {
  return null;
}
