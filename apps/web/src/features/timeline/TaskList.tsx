import type { TimelineSubject } from "./Timeline";

/** Placeholder. Owned by the pipeline workstream: tasks for a subject with add, complete and due dates. */
export function TaskList(_props: { subject: TimelineSubject }) {
  return null;
}
